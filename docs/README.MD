# Vision App

## Installation

1. Clone the repository
   ```bash
   git clone https://github.com/tu-usuario/vision-app.git
   cd vision-app
2. Create Virtual Enviroment
    ```bash
    Python -m venv myenv
    source myenv/bin/activate 
3. install the dependeces
    ```bash
    pip install -r requirements.txt


## status 
# Estado Actual y Próximos Pasos

## Resumen del Estado Actual

1. **Estructura del Proyecto Configurada**:
   - Carpetas: `data`, `src`, `tests`, `docs`.
   - Archivos: `main.py`, `utils.py`, `processing.py`, `test_processing.py`, `README.md`, `requirements.txt`, `.gitignore`.

2. **Código Inicial Escrito**:
   - `main.py`: Carga una imagen, la procesa y muestra el resultado.
   - `processing.py`: Contiene la función `process_image` que convierte una imagen a escala de grises, la desenfoca y detecta bordes.
   - `utils.py`: Función auxiliar para cargar una imagen.

3. **Documentación Inicial en `README.md`**:
   - Instrucciones de instalación, uso y pruebas.

## Próximos Pasos

1. **Definir el Objetivo de la Aplicación de Visión Artificial**:
   - Clarificar qué tarea específica realizará la aplicación 

2. **Crear Conjuntos de Imágenes para Pruebas**:
   - Decidir cómo se organizarán las imágenes de prueba 
   - Automatizar la creación y gestión de conjuntos de datos para pruebas.

3. **Implementar y Mejorar las Pruebas**:
   - Definir tests más detallados y variados para asegurar que la aplicación funcione correctamente en diferentes escenarios.

## Paso 1: Definir el Objetivo de la Aplicación de Visión Artificial

Para avanzar, necesito que respondas a algunas preguntas:

1. **¿Cuál es el objetivo principal de la aplicación de visión artificial?**
   - ¿Qué tipo de procesamiento de imágenes necesitas? 
